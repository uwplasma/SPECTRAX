# Energization mechanism, tail validity and matched optimization

This follow-up tests the scientific gaps identified in `publication_assessment.md`.
It uses released SOLVAX 0.20.0 and the same SPECTRAX RK4 equations. No alternate
collision model or custom plasma adjoint is introduced. Compressed raw mechanism
reports, hashes and a sampled-data summary are in `benchmarks/results/acceleration`.
Read a report with `json.load(gzip.open(path, 'rt'))`; the summary and figure are
regenerated by `python benchmarks/acceleration_summary.py`.

## What the original energy optimum changes

The additional [40,60] averaged electron energy is approximately 51.3% bulk and
48.7% internal. These partitions use Simpson integration of saved unit-time
samples; the original RK-stage objective remains the reference for its exact
reported optimization gain. Species field work is multiplied by the normalization
used in the actual Vlasov RHS, with the same spectral mask. The electron work
budget closes within 2.2e-17 across the eight diagnostic cases. This verifies
accounting, not entropy production or irreversible heating. Small CPU/GPU sampled
moments and integrated work agree within 2.8e-17.

![Mechanism and validity](figures/energization_mechanism_comparison.png)

**Caption.** (a) Added averaged total, bulk and internal electron energies for the
original energy-optimized controls, 16²/H4. (b) Added normalized smooth-tail gain
of those frozen controls over their baseline, using the fixed threshold 5 times
initial mean thermal energy and width 0.5 times that energy. All initial tails
are subtracted separately. (c) Maximum cell negative-mass fraction at T70,
measured with 48³ velocity quadrature. (d) Maximum absolute electron work-budget
residual over the sampled trajectory. The H4 case uses 16²; all higher-H cases use
24². Case ordering in (d) is recorded in `mechanism_summary.json`. Curves join
measured resolutions only. This is a diagnostic comparison, not a tail-optimized
acceleration result. Global negative mass can be tiny while local negative mass
is unacceptable.

## Why collision normalization matters

The implemented filter rate per velocity direction is
`nu*i*(i-1)*(i-2)/[(H-1)*(H-2)*(H-3)]`. It directly preserves density, momentum
and kinetic energy, but damps higher moments. Holding nu fixed while increasing H
changes the operator on shared modes. Thus earlier fixed-nu H4→H6 comparisons
combine filter sensitivity with numerical refinement.

Holding the shared rates fixed instead requires H4/nu1→H6/nu10→H8/nu35.
The latter two cases agree closely in the tail benefit, but converge to nonzero
local negativity: optimized T70 mean negative mass 2.95439e-6 and worst-cell
fraction 1.59793e-4. Increasing H does not remove this fixed-filter defect. It is
not justified to label the resulting tail an accelerated physical population.

A distinct collisionless nu0 comparison has a better validity trend. The
energy-control tail benefit changes only 0.066% between H8 and H12; its q48→q96
quadrature change is around 1e-5. H12 still misses the stricter local-cell 1e-8
pilot tolerance at T60/T70. H16 passes at all sampled validation times:

| Time | Baseline worst-cell fraction | Energy-control worst-cell fraction |
|---|---:|---:|
| 40 | 4.02e-14 | 1.28e-13 |
| 50 | 5.52e-12 | 6.40e-12 |
| 60 | 5.57e-11 | 1.74e-10 |
| 70 | 1.56e-9 | 4.66e-9 |

These are finite-quadrature checks on sampled times, not a continuous positivity
proof. Collisionless energy transfer is not irreversible collisional heating.
The old controls were optimized for nu1 energy gain; their nu0 evaluation is a
new setting and does not constitute a nu0 tail optimization.

## Tail-control search and cost

The collisionless baseline tail gradient changes 8.59e-5 relatively between
24²/H12 and H16, with dt = 0.05. A 16²/H8 proposal gradient at dt = 0.1 differs
from the H16 reference by 2.15%, within the predeclared 5% pilot gate, while one gradient
costs 3.92 s versus 141.78 s at H16. The cheaper discrete control search converges
in 24 iterations, from averaged tail gain 0.000152517 to 0.000179408, in 110.65 s
excluding compilation and postprocessing. This 17.63% improvement is a **coarse
candidate**, whose endpoint local-negativity check fails. Only fine-grid
re-evaluation, gradient/refinement checks and held-out-time diagnostics can
support its physical interpretation; they must be reported separately.

## Frozen-control fine validation

The candidate is re-evaluated in the collisionless model on 24²/H16 with
1200 RK4 steps. Baseline gain is 0.000152686146 and optimized gain is
0.000179567699: an added 2.68816e-5 in units of initial electron thermal energy,
or **17.61% of the baseline gain**. The absolute added energy is 1.26007e-6,
only **0.002688% of initial thermal energy**; this is a small finite-time effect.
It is not a 17.61% increase of the total suprathermal population or total energy.

| Frozen-control check | Relative change of added gain |
|---|---:|
| Halve timestep, 1200 → 2400 steps | 2.19e-9 |
| Increase spatial grid, 24² → 32² (2400 steps) | 3.06e-5 |
| Increase velocity resolution, H16 → H20 | 2.81e-7 |

All scalar gates pass. The fixed seed-3 directional derivative changes by
4.99e-5 relatively (absolute 8.99e-13) between H16 reverse AD and H20 forward
JVP. This is one direction, not a full-gradient refinement study. The fine
optimized gradient norm is 8.68e-8; the coarse candidate is not declared a
stationary or globally optimal solution of the fine problem. All source hashes
remain unchanged during validation. Local distribution validity and held-out
diagnostics are assessed separately below.

## Fine local validity and energy partition

At 24²/H16, both controls pass the 1e-8 local-cell negative-mass gate at
T = 0, 40, 50, 60 and 70. The worst optimized value is 4.66e-9 at T = 70;
the baseline is 1.56e-9. Global q48→q96 smooth-tail quadrature changes remain
below 1.01e-5 relatively throughout the sampled trajectory, and electron work
balance closes below 1.85e-17. These finite-node, sampled-time checks support
this resolved discrete result; they do not prove continuous positivity.

The tail optimum's additional averaged total kinetic energy is 5.47e-6, composed
of **+9.85e-6 bulk and −4.38e-6 internal energy**, using sampled Simpson averages.
Thus this lab-frame tail objective favors increased bulk motion while reducing
internal energy relative to baseline. This is an important physical limitation:
these results support constrained suprathermal lab-frame energization, not
increased thermal energy or established local nonthermal acceleration.

![Tail control and fine validation](figures/tail_control.png)

**Caption.** Coarse fixed-energy phase search, frozen-control fine-grid gain,
time/space/Hermite refinement relative to prospective tolerances, and fine
sampled tail histories through T = 70. Each control subtracts its own initial
tail; threshold and width are held fixed. Coarse negativity failure is retained;
fine sampled validity is reported above. The fine result is reevaluated, not
reoptimized. The time-history panel uses saved samples, while scalar optimization
and validation gains use RK-stage integration. [Vector figure](figures/tail_control.pdf).

## Threshold sensitivity and held-out interval

All three fixed thresholds retain positive benefit without retraining or
threshold selection. Width remains 0.5 times initial mean thermal energy.
These are q96, unit-time sampled Simpson integrals, not RK-stage evaluations.
Held-out [60,70] is a uniform average relative to each control's own T0 tail;
it is not the energy gained since T60.

| Threshold / initial mean thermal energy | Added training gain [40,60] | Added held-out gain [60,70] |
|---|---:|---:|
| 4 | 5.31693e-5 | 7.28653e-5 |
| 5 (optimized threshold) | 2.68817e-5 | 3.72834e-5 |
| 6 | 1.19861e-5 | 1.67737e-5 |

The q48→q96 benefit change is below 1.39e-4 relatively across these cases.
At threshold 5, sampled training benefit differs from exact RK-stage validation
by 4.14e-6 relatively; individual sampled gains differ more. No independent
held-out time-sampling convergence is claimed. These overlapping observations
of one optimized control set are not independent optimization starts.

[Global spectrum figure](figures/tail_spectrum_optimized.pdf) compares the saved
spatial-average distribution with instantaneous moment-matched isotropic and
covariance-matched Gaussian references at T0/50/60/70. It uses a fixed finite
energy range 0–12 times initial mean thermal energy and reports omitted-range
residuals; its integrals are distinct from full Gauss-Hermite tail quadrature.
Global Gaussian excess still permits mixtures of local flows and temperatures.
Raw spectral curves and threshold reports are archived alongside the controls.

## Matched AD/FD optimization

The primary comparison uses separate serial runs on one NVIDIA RTX A4000 at
16²/H4, 600 RK4 steps to T = 60, eight phase controls and seed 7. Both methods
minimize the same weighted [40,60] electron-energy objective from identical
initial phases, with fixed mode amplitudes and unrestricted periodic phases.
L-BFGS-B settings are identical, including `ftol=1e-12`, `gtol=1e-9` and a
30-iteration cap. Both converge by relative objective reduction after 29
iterations and 35 optimizer value/gradient calls. Their final objectives differ
by 2.03e-14, reaching a normalized averaged electron gain of 0.009531093361.

| Measurement | Reverse AD | Serial centered FD |
|---|---:|---:|
| Total optimization cost, including setup | 73.48 s | 221.39 s |
| Compilation | 15.20 s | 5.12 s |
| Warm optimization | 53.19 s | 183.25 s |
| All objective evaluations | 37 | 703 |
| Optimizer iterations / value-gradient calls | 29 / 35 | 29 / 35 |

These direct measurements give a 3.01× total-cost and 3.45× warm-optimization
FD/AD ratio. The FD run uses `--fd-reference none`: it never compiles or evaluates
AD, and neither run subtracts diagnostic time. Total cost equals measured
pipeline wall time, including launch/setup, compilation, warmup, FD calibration,
optimization, callbacks and incremental JSON I/O; final report writing and
plotting are excluded. Every FD perturbation counts as an objective evaluation.
The FD step of 1e-5 radians is selected only from preregistered baseline full and
directional FD self-consistency, with allowance `0.1*gtol + 1e-3*max(||g_FD||,1e-14)`.
No optimum-based tuning or AD reference enters this selection.

![Matched AD and FD optimization](figures/optimization_comparison_gpu.png)

**Caption.** Best accepted normalized averaged electron gain versus (a) total
optimization cost and (b) warm optimization wall time for the primary standalone
AD and FD runs. Total cost includes method-required compilation and calibration;
warm time starts at optimization. Both reach the same objective to 2.03e-14.
No diagnostic AD cost is incurred or subtracted in the FD run. These are one-seed,
one-device measurements against serial centered FD, not an optimal batched-FD
comparison or a universal speedup. Device utilization/process snapshots do not
measure SM occupancy or certify absence of contention between samples.
[Vector figure](figures/optimization_comparison_gpu.pdf).

Source: [primary timing summary](../benchmarks/results/acceleration/optimization_comparison_summary.json),
with full traces and provenance in
[standalone AD](../benchmarks/results/acceleration/optimization-t60-standalone-ad-seed7.json.gz)
and [standalone FD](../benchmarks/results/acceleration/optimization-t60-standalone-fd-seed7.json.gz).
These results supersede the earlier comparison that subtracted FD diagnostic AD
cost. The measured 3.01× end-to-end ratio is distinct from the earlier
128-control, T = 2, approximately 60× warm gradient benchmark.

All these results are reproducible numerical-control evidence. A global tail
increase or excess relative to a global Maxwellian can still reflect a mixture
of local flows, temperatures or anisotropies. Local nonthermal acceleration or
a newly identified mechanism requires additional evidence.
